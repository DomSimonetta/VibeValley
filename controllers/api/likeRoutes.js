const express = require('express');
const router = express.Router();
// const likeController = require('../likeController');

// // POST to like a song
// router.post('/:songId/likes', likeController.likeSong);

// // GET likes on a song
// router.get('/:songId/likes', likeController.getLikesOnSong);

// // DELETE a like from a song
// router.delete('/:songId/likes/:likeId', likeController.unlikeSong);

module.exports = router;
